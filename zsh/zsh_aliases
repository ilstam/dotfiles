# general aliases
alias d='cd /media/data/'
alias pingg='ping www.google.com'
alias mysensors='sensors | egrep -A2 "k10temp|radeon"'
alias teithevpn='cd $DOTFILES/vpn/teithe/ && sudo openvpn tux-full-udp.ovpn'

# change the default behavior of these commands
alias ls='ls --group-directories-first --color=auto'
alias packer='packer --noedit --auronly'

# applications aliases
alias p2='python2'
alias p3='python3'
alias ip2='ipython2'
alias ip3='ipython3'

# ssh connections
alias aetos='ssh istamati@aetos.it.teithe.gr'

# when using sudo, use alias expansion (otherwise sudo ignores your aliases)
alias sudo='sudo '

# easy clone my git repos
clone() { git clone https://github.com/Ilias95/$1 }
cloneaur() { git clone ssh://aur@aur4.archlinux.org/$1.git }

# pacman specific
if [ -f /var/log/pacman.log ]; then
	alias up='sudo pacman -Syu'
	alias orphansls='pacman -Qdt'
	alias orphansrm='sudo pacman -Rns $(pacman -Qtdq)'
	alias cacheclean='sudo pacman -Sc'
	alias fromaur='pacman -Qm'
fi

# apt specific
if [ -d /var/log/apt/ ] || [ -f /var/log/apt.log ]; then
	alias up='sudo apt-get update && sudo apt-get upgrade'
fi

# systemd specific
alias s1='systemctl poweroff'
alias s2='systemctl suspend'
alias s3='systemctl reboot'

# end in the letter 'd' to avoid conflicts with zsh builtins such as enable
alias listd='find /etc/systemd/system -mindepth 1 -type d | xargs ls -l --color'

statusd() { sudo systemctl status $1.service; }
startd() { sudo systemctl start $1.service; }
stopd() { sudo systemctl stop $1.service; }
restartd() { sudo systemctl restart $1.service; }
enabled() { sudo systemctl enable $1.service; }
disabled() { sudo systemctl disable $1.service; }
maskd() { sudo systemctl mask $1.service; }
unmaskd() { sudo systemctl unmask $1.service; }

listmasked() { systemctl list-unit-files | awk '$2 == "masked" {print $1}' }
