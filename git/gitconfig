[user]
    email = stamatis.iliass@gmail.com
    name = Ilias Stamatis

[core]
    editor = vim

[push]
    default = simple

[merge]
    tool = vimdiff

[alias]
    amend = !sh -c 'git commit --amend --date=\"$(date)\"'

    co = checkout
    st = status
    br = branch

    unstage = reset HEAD
    last = log -1 HEAD

[credential]
    helper = cache

[sendemail]
    smtpEncryption = tls
    smtpServer = smtp.gmail.com
    smtpUser = stamatis.iliass@gmail.com
    smtpServerPort = 587
